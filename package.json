{
  "name": "electron-app",
  "version": "1.0.0",
  "main": "main.js",
  "dependencies": {
    "asar": "^2.0.1",
    "electron": "^5.0.0",
    "electron-packager": "^13.1.1",
    "mqtt": "^2.18.8",
    "serialport": "^7.1.5"
  },
  "devDependencies": {
    "electron-rebuild": "^1.8.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/electron.cmd .",
    "set_env": "npm install --vs2015 -g windows-build-tools",
    "mybuild": "./node_modules/.bin/electron-rebuild",
	"pack_win": "./node_modules/.bin/electron-packager . --overwrite --out=release sample --platform=win32 --arch=x64",
	"pack_mac": "./node_modules/.bin/electron-packager . --overwrite --out=release sample --platform=darwin --arch=x64",
	"pack_x64": "./node_modules/.bin/electron-packager . --overwrite --out=release sample --platform=all --arch=x64",
	"pack_all": "./node_modules/.bin/electron-packager . --overwrite --out=release sample --platform=all --arch=all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
